# 序 — current application and release rules

- Canonical source: `rockru4211-lang/pantryflow`, `main`. Work on the current full application under `app/` and `lib/`; Supabase migrations/functions live under `supabase/`.
- Production hosting is Cloudflare Workers at `https://pantryflow.rockru4211.workers.dev/`. The user has stopped previews. Historical Sites/build compatibility files are not a publishing target; use the canonical repository and the existing Cloudflare worker.
- Preserve the approved shell, eight identities, role checks, source files and historical operations. Latest accepted Notion decisions and explicit user instructions govern scope. Do not revive the historical `pilot-v1` frontend.
- Never commit credentials, signed private-file URLs, QA access files, local build output or `.env.local`. Password advice is local only; staff PIN rules are independent.
- Run typecheck, relevant tests, lint and build. Schema changes also require role/denial/idempotency regression and preservation checks. Import changes must cover first use, partial success/retry and removed-file re-import with store/history protection. `npm run verify:source` checks the release contract; `npm run verify:release` requires a clean tree and the exact canonical main SHA. After publication, `EXPECTED_BUILD_SHA=<release SHA> npm run verify:production` must verify the served version and all linked JavaScript/CSS, including PDF parser/worker assets. A successful build alone is not deployment verification.
- Build with `NEXT_PUBLIC_BUILD_SHA` equal to the verified source commit. Save and deploy that same commit to the existing original URL. Publication remains subject to the user's scope and authorization.
- For database changes, confirm the required migrations are applied to the production Supabase project before marking the release complete. Run isolated rollback fixtures rather than importing/deleting the user's real data as a test. A clean local database test does not prove production migrations were applied.
- Old GitHub Pages contains only a redirect to Sites. Its workflow must never publish application code. Keep old branches/history as recovery evidence; do not force-push or delete unreviewed work.
