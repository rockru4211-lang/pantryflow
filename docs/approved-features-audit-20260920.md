# 序：功能定案與部署核對（2026-09-20）

適用範圍：獨立餐廳、無 ERP、兩門市；9/22 上線先收斂盤點、進貨、借貸、調撥。依使用者要求不開預覽。

## 核對依據與結論

目前不能判定「已定案功能全部部署完整」。本次直接依據使用者附上的三頁定案圖，以及 9/15 的 `xu-ux-audit.html` 檢查文件。尚未取得名為「序 功能調整」的完整原對話，因此舊檢查文件只作補充比對，不能代替其後全部定案，也不把不同於舊圖的地方一律改回舊版。

修正前 Cloudflare 正式站的應用程式資產對應 main `d3c4ff52f25e30935c4b0ada8d1e63c5e6e7de10`。PR #40 確實部署，但將左滑入口改成直接按鈕，沒有忠實保留最新定案操作。這是實作偏差，不能只歸因於快取。

## 最新三頁定案圖

| 定案項目 | 修正前 | 本次修正 |
| --- | --- | --- |
| 匯入檔案左滑顯示紅色「移除」 | `overflowX:hidden` 阻止橫向滑動，改成可見文字按鈕 | 恢復原生水平滑動與紅色移除區，增加垃圾桶圖示與操作提示 |
| 一次移除整份資料及該批建立品項 | PR #40 已接整批 RPC；使用者不需逐項移除 | 保留單次確認、重複送出防護、成功後刷新；不是只刪檔案列表 |
| 匯入盤點總覽 | 自動開啟最新一份明細 | 先呈現檔案清單，顯示檔名、匯入時間、目前門市、項數 |
| 盤點設定「更多操作」 | 頁面直接展開歷史清單，右上角直接開重建確認 | 底部卡片與右上角開同一組更多操作，包含匯入總覽與重建入口 |
| 營業中／已停用門市分組 | 停用門市藏在收合區；缺營業中門市數量標題 | 兩組直接顯示並標明數量 |
| 停用門市左滑刪除 | 原本已有滑動與後端 `can_delete` 條件 | 保留：僅可刪除的停用門市提供刪除；有歷史的門市保留查看／恢復 |

匯入總覽仍依目前門市讀取；切店後檢查該店的匯入批次。圖上跨店檔案示例不能作為擴張資料權限的依據。整批移除保留已完成歷史、其他有效匯入仍使用的品項及其他門市使用資料。此輪沒有刪除任何使用者測試資料。

## 9/15 文件補充比對

以下「已有實作」表示目前原始碼可核對到，並非登入真實門市後的操作驗收。

| 文件項目 | 目前核對結果 | 來源 |
| --- | --- | --- |
| 首頁三張分類色卡與盤點狀態入口 | 目前首頁採角色作業列表，與舊三卡規格不一致；待完整後續定案確認，不宣稱完成 | `app/pilot/role-home.tsx` |
| 歸還日留白、選填、未還跨月保留 | 已有實作；草稿預設空白，查詢保留所有 OPEN 紀錄 | `transfers-workspace.tsx`、migration `20260918220500` |
| 借貸／調撥分頁與共用月份 | 已有實作；兩類紀錄分開 | `app/pilot/transfers-workspace.tsx` |
| 次要「查看金額」可展開隱藏 | 舊規格未完整對上；現版有調撥金額，但沒有該開關 | `app/pilot/transfers-workspace.tsx` |
| 已盤清單摘要、點列看明細、保留人員／時間 | 已有摘要與逐筆明細，非任意修改已完成盤點 | `app/pilot/count-details.tsx` |
| 缺貨搜尋與安全庫存依據 | 已有搜尋、尚缺／可用／設定與單位；300 項實際操作未驗收 | `app/pilot/role-home.tsx` |
| 注意原因「其他」同表單輸入 | 已有實作 | `app/pilot/expiry-waste-workspace.tsx` |
| 風險位置兩個動作 | 已有登記廢棄、其他狀況 | `app/pilot/expiry-waste-workspace.tsx` |
| 貨單單張不問、多張預設分開、同單多頁次要選項 | 已有實作，提交前有建單數量 | `app/pilot/receiving-workspace.tsx` |
| 整張貨單／品項卡一次編輯 | 現場角色已有卡片編輯；行政表格沿用同一編輯器，不能宣稱所有角色畫面相同 | `receiving-workspace.tsx`、`receipt-card-editor.tsx` |
| 商品對應併入品項卡 | 已有實作 | `app/pilot/receipt-card-editor.tsx` |
| 收貨完成摘要與未入庫真實狀態 | 有精簡摘要／查看完整紀錄；未計入庫存的說明仍藏在展開明細，未完全符合摘要內提示的舊要求 | `app/pilot/receiving-workspace.tsx` |
| 本班交接與實際盤點進度 | 已有實作，使用任務數量顯示已盤／本次品項與完成區域 | `records-workspace.tsx`、`work-feed.tsx` |
| 待辦逐筆直達、完成後退出 | 已有以紀錄 ID 接續的待辦列表，OPEN 借貸納入、完成調撥不列待處理 | `app/pilot/work-feed.tsx` |
| 作業紀錄共用月份、分類與一致列表 | 已有實作 | `app/pilot/work-feed.tsx` |
| 單店首頁與明細範圍 | 現版首頁讀取目前 store.id 的數值；真實兩店不同筆數對照仍未驗收 | `app/pilot/role-home.tsx` |
| 借貸搜尋直接接登記 | 已接門市、品項與單位；空品項結果已排除 | `app/pilot/transfers-workspace.tsx` |
| 無 ERP 設定頁移除空轉儲存按鈕 | 已有實作 | `app/pilot/business-settings.tsx` |
| 首頁移除未提供的成本／毛利率 | 尚未對上舊規格；現版仍顯示未提供 | `app/pilot/role-home.tsx` |

## 上線尚未能標示完成的部分

- PR #39 仍為 open、未合併：Cloudflare 網址及進貨異常處理修正未隨本次 UI 修正部署。
- 正式 Auth 回呼白名單、真實登入／邀請、正式 OCR 額度與辨識仍缺驗收證據。
- 真實兩門市的盤點、收貨入庫、借還及調撥扣增庫存完整流程，不能以本機測試或下載到部署資產取代。
- 左滑使用既有原生捲動及 CSS；本次不開預覽，尚未以登入帳號在使用者手機觸控驗收。

本次只改前端入口與呈現，不修改資料庫、角色授權、庫存規則或部署 PR #39。合併及正式資產版本核對結果以本次 PR 和開發計畫更新為準。
