# 序｜2026-09-22 上線收斂

2026-09-20 使用者決定：停止預覽與額外功能擴充，保留已確認版型，集中完成盤點、進貨、借貸、調撥。正式網址為 https://pantryflow.rockru4211.workers.dev/ 。GitHub main 為 Cloudflare 程式來源，不再要求 Sites 鏡像。

## 四项必要驗收

| 項目 | 上線必要流程 | 必須留下的證據 |
| --- | --- | --- |
| 盤點 | 使用目前有效品項開盤、填數量、保留未完成進度、提交、查看結果；移除匯入品項不再阻擋開盤 | 操作角色、盤點編號、提交紀錄；重送不能產生第二次入帳 |
| 進貨 | 拍照上傳、辨識、核對、確認收貨；在原貨單處理少貨、多貨、未收到等異常 | 貨單編號、原圖、確認紀錄；辨識失敗不可被算為完成，重試不重複入帳 |
| 借貸 | 建立借入／借出、追蹤未結清、歸還或換貨、結清後回查 | 雙方、品項、單位、數量、剩餘數量與歷史；重送不重複扣加庫存 |
| 調撥 | 依現有權限選來源／目的門市及品項數量，完成調撥、回查兩端庫存 | 同一筆調撥的兩端紀錄；不可跨商家、不可部分成功；重送不重複扣加庫存 |

四項均沿用現有角色權限與資料流程。本次不修改庫存 SQL、權限模型或歷史交易；以上是正式環境驗收條件，尚不是通過紀錄。

## 本次程式修正

- 登入／驗證／重設密碼／邀請回跳、PWA 啟動與舊書籤導向，統一 Cloudflare 正式網址。
- 進貨引導回到原貨單「修改到貨／處理異常」，避免要求現場人員回首頁尋找另一入口。
- OCR 新失敗紀錄區分 429 用量或請求限制、暫時服務忙碌、設定異常及無法取得品項；前端顯示可採取的動作，不顯示供應商原始錯誤。
- 429 不一律判定為永久額度耗盡；既有有上限的重試機制保持不變。舊錯誤紀錄保留相容提示，不回寫歷史。
- 更新來源檢查與工作規則，停止預覽／Sites 發布要求；新增錯誤分類回歸測試。

## 部署與驗證順序

1. 修正分支通過現有 GitHub CI：完整遷移重建、RLS/RPC/schema、型別、lint、build、測試。本機環境在檢查開始時離線，不把未取得結果的檢查算作通過。
2. 在 Supabase hosted Auth 確认 Site URL 與 redirect allow list 已允許正式 Cloudflare 網址及 recovery/signup/google/invite。修改 config.toml 不代表已修改 hosted Auth。
3. 部署 process-receipt-ocr 及新共用 ocr-errors.ts，保持 verify_jwt=true。已確認目前線上 v5 主程式與修改前 GitHub 主程式一致；尚未部署此變更。
4. 合併已驗證程式，確認 Cloudflare 部署 SHA 與 GitHub main 一致，再驗證正式登入回跳及手機啟動網址。既有主畫面捷徑若仍開舊站，須從正式網址重新加入。
5. 用授權測試門市完成上表四項流程及斷線／重送，驗證紀錄與庫存。不要拿正式營運庫存建立未經同意的測試交易。

## 尚未解除的上線阻擋

- OCR：最近可查的 receipt OCR 失敗主要為 Gemini 429；沒有證據證明目前額度已恢復。錯誤提示修正不等於恢復辨識服務，需確認服務帳戶額度並以一張授權貨單驗證。
- 正式 Auth 回跳 allow list 尚未取得管理設定驗證；Cloudflare 自動部署流量／部署 SHA 尚需確認。
- 四項功能尚缺本輪正式環境登入後的完整端到端驗收，不將既有 CI 或公開示範列為正式驗收完成。

不在本次範圍：新預覽、版型重做、新報表、收費串接與其他功能擴充。保留現有功能，不做無關刪除。
