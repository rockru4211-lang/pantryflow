# 選用效期入口驗證（2026-09-09）

依據 Notion `PF-OPTIONAL-EXPIRY-ENTRY-20260909`。接續原網址實際部署 v88，來源 `d260c1d69ce55d53ee5263571fa419349a2a8256`，發布前再次核對仍為 v88。

## 修改

- 盤點區域、進貨人工核對頁，各一個次要「加入效期提醒」按鈕。使用同一個精簡表單；原工作區、輸入、暫存佇列及返回路徑保持掛載。
- 伺服器依登入者、門市、盤點範圍／貨單列取得品名、單位及已確認商品關聯。沒有商品對應不推測；盤點數量不作為批次数量。缺儲放區可確認未分類，不要求建檔設定。
- 由同一品項選擇既有批次或明確補記另一批次；同一貨單列也能保存不同批次。保存與取消都返回原閱讀位置。
- 使用固定操作 ID 防重複；不確定回應後即使修改輸入也不另建批次。已識別提醒沿用 ID，更正追加 revision，保留原始日期。既有效期清單與完成歸檔讀取有效版本。
- 權限沿用工作來源：盤點為門市現場角色，独立餐廳進貨為 ADMIN／LOGISTICS，連鎖進貨為 ADMIN／SUPERVISOR；其他門市及未授權來源拒絕。

## App 操作驗證

使用 390 × 844 手機尺寸瀏覽器操作，並非實體手機硬體驗收。

### 盤點（QA0907UI）

- 主管由原入口開始下一次盤點，區域輸入麵粉 7.5 公斤、鮮奶油 2 瓶。
- 開啟提醒、選品填日期後取消：沒有建立提醒，兩個數量仍在。
- 保存麵粉效期 2026-09-10，帶入驗證冷藏區 A；返回仍為原區域、原數量。
- 重開同批並重複保存，資料庫仍只有一笔；員工重新登入後讀到同一提醒與數量，也可沿用保存。
- 員工完成此區域，未分類牛奶填 4 瓶，沒有設定效期仍完成整筆。14:37:11 完成 2 區域、3 筆實盤。
- 共用效期首頁預告可見麵粉提醒。
- Count session: `e99867ed-3f45-4d63-9848-83711def2366`；expiry: `2fee4be8-44a9-4e59-b6bb-c6e8f981be9a`。

### 進貨（QA0908RECEIPT）

- 新貨單副本從原 App 上傳入口保存成功；新 OCR 於 14:39 起執行，3 次嘗試皆遇 `GEMINI_429`，免費層 `gemini-3.6-flash` 請求額度 20 已達限制。原圖、失敗紀錄保留；沒有改模型、金鑰或辨識流程。
- 受外部額度限制，效期 UI 驗證改用明確標記的 **既有 OCR 回放資料**。獨立批次 `QA-EXPIRY-REPLAY-20260909`，來源為先前已辨識成功的 HEIC 測試貨單；provider=`QA_RECORDED_FIXTURE`，原始成功與失敗紀錄均不改寫。這不是新的 OCR 成功，亦不宣稱本輪新照片辨識通過。
- 後勤在原核對頁修改第 1 項規格為「QA 效期入口核對測試」，開啟、取消提醒後仍保留修改。取消前後 scrollTop 均為 350。
- 保存 Broken lump 碎蟹肉的 2026-09-11 包裝效期，確認未分類；沒有商品對應也能保存，product_id 與 lot_id 保留 null。保存後原修改仍在，scrollTop 仍為 350。
- 同一貨單品項補記另一批 2026-09-12，兩筆分開；重複保存既有第一批仍只有兩筆。
- 原下一步／儲存並確認收貨完成 3 列；其餘兩個品項未設定效期也能完成。14:52:10 確認收貨；仍各 1 CAN，沒有把未對應資料計入庫存。
- 後勤登出、重新登入，預告可見 9/11、9/12 兩批，作業紀錄可再次開啟已確認收貨與三筆明細。
- 新 OCR 失敗批次：`679fee72-755e-448f-baf6-f8fc2f619cfd`。
- 回放驗證批次：`c7dbf735-8acb-497f-8192-c3f4c8d020d6`；來源 run `e03fa0d2-0d19-4a07-911f-e7034f817724`。
- 提醒：`7b6f2e3c-d249-482d-b7b1-70669ad61b86`、`590fbb57-5477-43c1-9287-a0451beab890`。

## 自動與資料驗證

- 58 項既有 Node 測試通過；TypeScript、變更檔案 ESLint 通過。
- SQL 交易回滾測試：`context-expiry.sql`、`context-expiry-batches.sql`、`expiry-waste.sql`、`expiry-receipt-integration.sql` 通過。
- 涵蓋來源與門市權限、未對應商品、不洩漏盤點參考量、重試、同品項／同貨單列不同批次、日期更正歷史、過期版本拒絕、已完成提醒不重開、有效日期歸檔、未知區域及原數量不變。
- 新資料表位於 private、啟用 RLS 並拒絕前端直接存取；公開 RPC 為 invoker，private 實作逐次驗證權限。安全 advisors 未報本輪新增公開 definer；既有全站提醒另存原狀。
- 既有 342 商品、667 實盤、12 原圖文件保留。排除本輪新測試資料後：count_entries MD5 `0a2f9b8fe4d091d902eeff3a6e0a1ed1`，receipt_documents MD5 `14c50ca1b4e55f84284f3f9f976a4a0b`，與修改前完全一致。
- 測試資料保留，不清除使用者的門市、歷史盤點、原圖或提醒。新 OCR 額度受限為外部尚未排除事項。
