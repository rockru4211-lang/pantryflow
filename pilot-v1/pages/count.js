import{emptyState,escapeHtml}from'../components/layout.js';

const statusLabel={IN_PROGRESS:'盤點中',REVIEWING:'待主管處理差異',CLOSED:'已完成'};
const reasonOptions=[['INPUT_ERROR','輸入錯誤'],['MISSED_OR_WRONG_ZONE','漏盤／區域錯誤'],['WASTE_NOT_RECORDED','報廢未記錄'],['TRANSFER_NOT_RECORDED','調撥未記錄'],['RECEIPT_NOT_RECORDED','進貨未記錄'],['OTHER','其他']];

function accessFor(role,legacyCanManage=false){if(role==='STAFF')return{kind:'staff',canCount:true,canConfigure:false,canReview:false};if(role==='ADMIN'||role==='SUPERVISOR')return{kind:'manager',canCount:true,canConfigure:true,canReview:true};if(role==='LOGISTICS')return{kind:'logistics',canCount:false,canConfigure:false,canReview:false};if(role==='OWNER')return{kind:'owner',canCount:false,canConfigure:false,canReview:false};return legacyCanManage?{kind:'manager',canCount:true,canConfigure:true,canReview:true}:{kind:'staff',canCount:true,canConfigure:false,canReview:false}}
function focusSession(data){return data.focusSession||data.sessions?.find(session=>['IN_PROGRESS','REVIEWING'].includes(session.status))||data.sessions?.[0]||null}
function completedZones(data,session){return data.progress.filter(item=>item.session_id===session?.id&&item.status==='COMPLETED').length}
function taskAssignments(data,session){const snapshot=session?.snapshot?.zones;if(Array.isArray(snapshot)&&snapshot.length)return snapshot.map(item=>({zone_id:item.zone_id,product_id:item.product_id}));return data.assignments}
function taskZones(data,session){const ids=new Set(taskAssignments(data,session).map(item=>item.zone_id));return data.zones.filter(item=>ids.has(item.id))}
function totalItems(data,session=focusSession(data)){return taskAssignments(data,session).length}
function differenceCount(data){return data.discrepancies.filter(item=>item.status!=='RESOLVED').length}
function roleIntro(label,copy){return `<div class="count-role-intro"><span>${escapeHtml(label)}</span><small>${escapeHtml(copy)}</small></div>`}
function scheduleBadge(settings={}){const label={DAILY:'每日自動',WEEKLY:'每週自動',MONTHLY:'每月自動',MANUAL:'臨時盤點'}[settings.count_frequency]||'臨時盤點';return `<span class="count-mode-badge">${label}</span>`}
function countStats(items){return `<div class="count-stat-grid">${items.map(([value,label])=>`<div><strong>${escapeHtml(value)}</strong><small>${escapeHtml(label)}</small></div>`).join('')}</div>`}
function emptySetupForStaff(){return emptyState('主管尚未完成盤點設定','盤點區域與品項設定完成後，任務會自動出現在這裡。')}

function blindCountPage(data,context){
  const zone=data.zones.find(item=>item.id===context.zoneId);
  const session=data.sessions.find(item=>item.id===context.sessionId)||focusSession(data),assigned=taskAssignments(data,session).filter(item=>item.zone_id===context.zoneId).map(item=>data.products.find(product=>product.id===item.product_id)).filter(Boolean);
  return `${roleIntro('員工','只輸入現場數量')}<div class="count-heading"><div><h2 class="page-title">${escapeHtml(zone?.name||'區域盤點')}</h2><p>已填 ${assigned.filter(product=>data.drafts.some(draft=>draft.product_id===product.id)).length}／${assigned.length} 項</p></div><span class="count-icon">▣</span></div><p class="blind-note">盲盤模式｜不顯示上次數量、差異、成本或金額</p><p class="count-lock-note">每輸入一項就自動保存；離開後可以繼續。完成前會提醒漏填。</p><form id="blind-count" data-session="${escapeHtml(context.sessionId)}" data-zone="${escapeHtml(context.zoneId)}" class="list count-entry-list">${assigned.map(product=>{const draft=data.drafts.find(item=>item.product_id===product.id);return `<label class="card count-input-row"><span><strong>${escapeHtml(product.name)}</strong></span><span class="quantity-input"><input data-count-product="${product.id}" data-unit="${escapeHtml(product.count_unit)}" type="number" min="0" step="0.001" value="${draft?.quantity??''}" inputmode="decimal" placeholder="數量" required><b>${escapeHtml(product.count_unit)}</b></span><em data-save-state="${product.id}">${draft?'已自動保存':''}</em></label>`}).join('')}<button class="primary" type="submit">完成此區域</button><p class="error" data-error></p></form>`;
}

function paperGuidePage(data,{context,workflowSettings}){
  const session=data.sessions.find(item=>item.id===context.paperSessionId)||focusSession(data);
  if(!workflowSettings?.paper_transcription_enabled)return emptyState('此門市未啟用紙本謄寫','請返回盤點完成頁查看或匯出明細。');
  if(context.paperComplete)return `${roleIntro('現場人員','盤點後紙本作業')}<section class="count-complete-card"><span class="count-check">✓</span><h2>紙本謄寫已確認</h2><p>系統保留 App 盤點結果；紙本內容不需要再輸回 PantryFlow。</p></section><div class="count-actions"><button class="primary" data-export-count="${escapeHtml(session?.id||'')}" type="button">匯出本次盤點明細</button><button class="secondary" data-route="home" type="button">返回首頁</button></div>`;
  return `${roleIntro('現場人員','門市選配的盤點後作業')}<div class="count-heading"><div><h2 class="page-title">謄寫店內紙本</h2><p>依門市既有盤點表完成謄寫</p></div><span class="count-icon count-icon-orange">▤</span></div><section class="card paper-guide"><ol><li><strong>開啟本次盤點明細</strong><small>依區域與原盤點表順序查看</small></li><li><strong>將數量謄寫到店內紙本</strong><small>不需要把紙本內容再輸回 App</small></li><li><strong>完成後交由店長備存</strong><small>系統保留完成時間與經手人</small></li></ol></section><div class="count-actions"><button class="secondary" data-export-count="${escapeHtml(session?.id||'')}" type="button">先匯出盤點明細</button><button class="primary" data-paper-complete="${escapeHtml(session?.id||'')}" type="button">我已完成紙本謄寫</button></div>`;
}

function countDetailPage(data,{context,workflowSettings,role}){
  const session=data.sessions.find(item=>item.id===context.detailSessionId)||focusSession(data);
  const entries=data.entries.filter(item=>item.session_id===session?.id);
  const label=role==='STAFF'?'員工':role==='ADMIN'||role==='SUPERVISOR'?'店長／主管':role==='LOGISTICS'?'後勤':'Owner／管理者';
  if(!session)return emptyState('找不到盤點明細','請返回盤點頁重新選擇紀錄。');
  return `${roleIntro(label,'查看、匯出與列印')}<div class="count-heading"><div><h2 class="page-title">本次盤點明細</h2><p>${escapeHtml(session.started_at||'本次盤點')}・${entries.length} 筆紀錄</p></div>${scheduleBadge(workflowSettings)}</div><div class="card count-detail-list">${entries.length?entries.map(entry=>{const product=data.products.find(item=>item.id===entry.product_id);const zone=data.zones.find(item=>item.id===entry.zone_id);return `<article><span><strong>${escapeHtml(product?.name||'商品')}</strong><small>${escapeHtml(zone?.name||'未分類區域')}・${escapeHtml(product?.product_code||'')}</small></span><b>${escapeHtml(entry.quantity)} ${escapeHtml(entry.unit||product?.count_unit||'')}</b></article>`}).join(''):`<div class="empty compact-empty"><strong>本次尚無盤點紀錄</strong><p>完成區域盤點後，明細會顯示在這裡。</p></div>`}</div><p class="count-detail-note">原始實盤數量僅供查看；任何更正都必須新增事件並保留紀錄。</p><div class="count-actions"><button class="primary" data-export-count="${escapeHtml(session.id)}" data-export-kind="fillback" type="button">匯出原格式回填版</button><button class="secondary" data-export-count="${escapeHtml(session.id)}" data-export-kind="audit" type="button">匯出完整稽核明細</button><button class="secondary" data-print-count type="button">列印／另存 PDF</button></div>`;
}

function zoneProgress(data,session,{manager=false}={}){
  const zones=taskZones(data,session),assignments=taskAssignments(data,session);
  return `<section class="section"><div class="section-head"><h2>${manager?'區域進度與現場支援':'今天要盤的區域'}</h2><span>${completedZones(data,session)}／${zones.length}</span></div><div class="list">${zones.map(zone=>{const progress=data.progress.find(item=>item.session_id===session.id&&item.zone_id===zone.id);const itemCount=assignments.filter(item=>item.zone_id===zone.id).length;return `<article class="card zone-row count-zone-row"><span class="count-row-icon">${progress?.status==='COMPLETED'?'✓':'▣'}</span><div><strong>${escapeHtml(zone.name)}</strong><small>${itemCount} 項・${progress?.status==='COMPLETED'?'已完成':'等待盤點'}</small></div>${progress?.status==='COMPLETED'?'<span class="status">完成</span>':`<button class="secondary" data-open-count data-session="${session.id}" data-zone="${zone.id}">進入盲盤</button>`}</article>`}).join('')}</div></section>`;
}

function staffCompletion(data,{workflowSettings}){
  const session=focusSession(data);
  const paper=Boolean(workflowSettings?.paper_transcription_enabled),zones=taskZones(data,session);
  if(!session)return emptyState('今天尚未有盤點任務','主管完成第一次設定後，系統每天會自動準備。');
  return `${roleIntro('現場人員',paper?'盤點、謄寫與備存':'盤點與備存')}<div class="count-heading"><div><h2 class="page-title">盤點完成</h2><p>${escapeHtml(session.started_at||'本次盤點')}</p></div>${scheduleBadge(workflowSettings)}</div><section class="count-complete-card"><span class="count-check">✓</span><h2>${completedZones(data,session)}／${zones.length} 區域已完成</h2><p>本次共盤點 ${totalItems(data,session)} 項</p></section><div class="section-head count-after-title"><h2>盤點後作業</h2><span>依門市設定</span></div><div class="card count-after-list">${paper?`<button class="paper-task" data-paper-count="${session.id}" type="button"><span class="count-row-icon orange">▤</span><span><strong>謄寫店內紙本</strong><small>門市已啟用・不必再回填 App</small></span><b>去完成</b></button>`:''}<button type="button" data-export-count="${session.id}" data-export-kind="fillback"><span class="count-row-icon">⇩</span><span><strong>匯出原格式回填版</strong><small>依匯入表的位置與順序，方便謄寫或備存</small></span><b>›</b></button><button type="button" data-export-count="${session.id}" data-export-kind="audit"><span class="count-row-icon">☷</span><span><strong>匯出完整稽核明細</strong><small>含來源、經手人、時間與事件紀錄</small></span><b>›</b></button><button type="button" data-count-detail="${session.id}"><span class="count-row-icon">☷</span><span><strong>查看本次明細</strong><small>${totalItems(data,session)} 項・可列印或另存 PDF</small></span><b>›</b></button></div><div class="count-actions">${paper?`<button class="primary" data-paper-count="${session.id}" type="button">開始謄寫紙本</button>`:`<button class="primary" data-export-count="${session.id}" data-export-kind="fillback" type="button">匯出原格式回填版</button>`}<button class="secondary" data-route="home" type="button">返回首頁</button></div>`;
}

function staffCountPage(data,options){
  const session=focusSession(data);
  if(!data.products.length||!data.zones.length||!data.assignments.length)return `${roleIntro('員工','完成主管指派的盤點')}${emptySetupForStaff()}`;
  if(session?.status==='IN_PROGRESS')return `${roleIntro('現場人員','今天只要完成盤點')}<div class="count-heading"><div><h2 class="page-title">今日盤點</h2><p>${escapeHtml(session.started_at||'進行中')}</p></div><span class="count-icon">▣</span></div>${countStats([[`${completedZones(data,session)}／${taskZones(data,session).length}`,'區域進度'],[String(totalItems(data,session)),'盤點品項'],['盲盤','盤點方式']])}${zoneProgress(data,session)}`;
  return staffCompletion(data,options);
}

function discrepancyList(data){
  const pending=data.discrepancies.filter(item=>item.status!=='RESOLVED');
  return `<section class="section"><div class="section-head"><h2>差異原因確認</h2><span>${pending.length} 項待處理</span></div>${pending.length?`<div class="list">${pending.map(item=>{const product=data.products.find(product=>product.id===item.product_id);return `<article class="card discrepancy-card"><div><strong>${escapeHtml(product?.name||'商品')}</strong><p>期初 ${item.previous_quantity} → 實盤 ${item.estimated_quantity}，差異 ${item.difference}</p></div><form data-resolve-discrepancy="${item.id}" class="resolution-form"><label class="field">原因<select name="reason" required><option value="">選擇原因</option>${reasonOptions.map(([value,label])=>`<option value="${value}">${label}</option>`).join('')}</select></label><label class="field">處理方式<select name="action"><option value="RECOUNT">建立重盤事件</option><option value="CORRECTION">建立更正事件</option></select></label><label class="field">確認數量<input name="quantity" type="number" min="0" step="0.001" required></label><button class="primary">保存事件</button><p class="error" data-error></p></form></article>`}).join('')}</div>`:emptyState('沒有待處理差異','本次差異已完成整理。')}</section>`;
}

function importValidationCard(context={}){
  const preview=context.importPreview;
  const counts=preview?.counts||{matched:0,unmatched:0,duplicates:0,missingUnit:0,generatedCodes:0,defaultZones:0,invalid:0};
  return `<section class="card count-import-card"><div class="count-card-heading"><span class="count-row-icon">⇧</span><div><strong>匯入 Excel／Google 試算表</strong><small>系統自動辨識表頭、商品編碼、單位與區域</small></div></div><label class="secondary file-button count-import-button">選擇 CSV／Excel 檔<input data-count-import-file type="file" accept=".csv,.xlsx" hidden></label><div data-count-import-preview>${preview?`<div class="count-import-source"><strong>${escapeHtml(preview.fileName||'已選擇檔案')}</strong><small>工作表：${escapeHtml(preview.sheetName||'待確認')}・${preview.rows?.length||0} 列・${escapeHtml(preview.templateVersion||'')}</small></div>`:'<p class="count-import-empty">Google 試算表請下載成 Excel（.xlsx）或 CSV；不用先改成固定範本。</p>'}${countStats([[String(counts.matched),'已對應'],[String(counts.unmatched),'將新建'],[String(counts.generatedCodes||0),'自動編碼'],[String(counts.duplicates||0),'自動區分同名列']])}${preview?`<div class="count-import-sample">${preview.rows.slice(0,5).map(row=>`<div><span>${escapeHtml(row.zoneName)}</span><strong>${escapeHtml(row.productCode)}・${escapeHtml(row.productName)}</strong><small>原表第 ${row.sourceRow} 列・${escapeHtml(row.unit)}</small></div>`).join('')}</div><p class="${preview.canPublish?'helper':'error'}">${preview.canPublish?`可匯入：系統已補齊 ${counts.generatedCodes||0} 個商品編碼、${counts.missingUnit||0} 個單位、${counts.defaultZones||0} 個區域，並自動區分 ${counts.duplicates||0} 個同名位置；匯出仍回填原列。`:`尚不能匯入：無法辨識 ${counts.invalid} 列。`}</p><button class="primary" data-publish-count-import type="button" ${preview.canPublish?'':'disabled'}>確認並建立 ${preview.rows.length} 項</button><p class="error" data-count-import-error></p>`:'<p class="count-import-empty">選檔後先顯示預覽，確認才會正式建立品項與區域。</p>'}</div><p class="count-preview-note">員工不用設定：匯入完成後，系統會依區域與原表順序建立盤點清單。</p></section>`;
}

function managerCountPage(data,{context={}}){
  const active=data.activeSession||data.sessions?.find(item=>['IN_PROGRESS','REVIEWING'].includes(item.status))||null,session=active||focusSession(data),ready=Boolean(data.products.length&&data.zones.length&&data.assignments.length);
  if(!ready)return `${roleIntro('店長／主管','第一次只設定一次')}<div class="count-heading"><div><h2 class="page-title">先完成盤點設定</h2><p>完成後，員工每天打開就能直接盤點</p></div><span class="count-icon">1</span></div><section class="card count-onboarding-card"><strong>最快方式：匯入現有 Excel</strong><p>不用整理成固定範本。系統會辨識商品、單位與區域，確認後一次建立。</p></section>${context.importComplete?`<p class="helper">${escapeHtml(context.importComplete)}</p>`:''}${importValidationCard(context)}<div class="count-actions"><button class="secondary" data-feature="zones" type="button">沒有 Excel，手動建立區域</button></div>`;
  return `${roleIntro('店長／主管','今天盤點與差異處理')}<div class="count-heading"><div><h2 class="page-title">今日盤點</h2><p>${active?escapeHtml(statusLabel[active.status]||active.status):session?'今天已完成':'系統正在準備今日任務'}</p></div><span class="count-mode-badge">每日自動</span></div>${context.importComplete?`<p class="helper">${escapeHtml(context.importComplete)}；每日自動盤點已啟用。</p>`:''}${countStats([[`${completedZones(data,active)}／${taskZones(data,active).length}`,'區域進度'],[String(totalItems(data,active||session)),'盤點品項'],[String(differenceCount(data)),'差異待確認']])}${active?.status==='IN_PROGRESS'?zoneProgress(data,active,{manager:true}):''}${differenceCount(data)?discrepancyList(data):''}<section class="card count-manager-setup"><div><strong>盤點設定</strong><small>只在區域或品項改變時調整；不必每天發布任務。</small></div><div class="button-row"><button class="secondary" data-feature="zones" type="button">調整區域與品項</button><button class="secondary" data-feature="catalog" type="button">商品主檔</button></div></section>${session&&session.status!=='IN_PROGRESS'?`<div class="count-actions"><button class="primary" data-count-detail="${session.id}" type="button">查看本次盤點</button><button class="secondary" data-export-count="${session.id}" data-export-kind="audit" type="button">匯出稽核明細</button></div>`:''}`;
}

function logisticsCountPage(data){
  const session=focusSession(data),pending=differenceCount(data);
  return `${roleIntro('後勤','整理、檢討與分析')}<div class="count-heading"><div><h2 class="page-title">盤點資料工作台</h2><p>${data.sessions.length} 次盤點紀錄・資料依門市同步</p></div><span class="count-icon count-icon-blue">▥</span></div>${countStats([[String(data.sessions.length),'資料批次'],[String(pending),'差異待分類'],[String(session?1:0),'報表待產出']])}<section class="section"><div class="section-head"><h2>後勤工作</h2><span>不改原始實盤</span></div><div class="card count-work-list"><button type="button" data-print-count><span class="count-row-icon blue">☷</span><span><strong>整理盤點明細</strong><small>單位、批次、來源與缺漏檢查</small></span><b>›</b></button><button type="button"><span class="count-row-icon blue">⇄</span><span><strong>差異分類與檢討</strong><small>耗損、漏登、單位錯誤、移撥差異</small></span><b>›</b></button><button type="button"><span class="count-row-icon blue">▥</span><span><strong>建立分析報告</strong><small>跨店比較、趨勢與異常品項</small></span><b>›</b></button></div></section>${session?`<div class="count-actions"><button class="primary" data-print-count type="button">開始整理今日資料</button><button class="secondary" data-export-count="${session.id}" type="button">匯出分析明細</button></div>`:emptyState('尚無盤點資料','門市完成盤點後，資料會出現在這裡。')}`;
}

function ownerCountPage(data,{workflowSettings}){
  const session=focusSession(data),pending=differenceCount(data),paper=Boolean(workflowSettings?.paper_transcription_enabled);
  return `${roleIntro('Owner／管理者','看結論、決策與稽核')}<div class="count-heading"><div><h2 class="page-title">盤點管理摘要</h2><p>後勤整理後的營運結論</p></div>${scheduleBadge(workflowSettings)}</div>${countStats([[String(data.sessions.filter(item=>item.status==='CLOSED').length),'已完成盤點'],[String(pending),'重大異常'],[paper?'1':'0','紙本待確認']])}<section class="section"><div class="section-head"><h2>盤點政策</h2><span>目前設定</span></div><div class="count-policy-list"><span>盲盤</span><span>差異需原因</span>${paper?'<span>紙本謄寫追蹤</span>':''}<span>明細匯出</span></div></section><section class="section"><div class="section-head"><h2>管理重點</h2><span>${session?'2':'0'} 項</span></div>${session?`<div class="card count-work-list"><button type="button"><span class="count-row-icon purple">▥</span><span><strong>查看後勤分析結論</strong><small>只呈現重大差異與改善重點</small></span><b>›</b></button><button type="button" data-print-count><span class="count-row-icon purple">✓</span><span><strong>門市稽核追蹤</strong><small>盤點紀錄、選配紙本與差異處理</small></span><b>›</b></button></div>`:emptyState('目前沒有管理異常','完成盤點後會顯示整理後的結論。')}</section>${session?'<div class="count-actions"><button class="primary" data-print-count type="button">查看本月管理總結</button></div>':''}`;
}

export function countPage(data,{role='STAFF',workflowSettings={},context={},canManage=false}={}){
  const access=accessFor(role,canManage);
  if(context.sessionId&&context.zoneId){if(!access.canCount)return emptyState('此角色不執行現場盲盤','請返回角色專屬的盤點頁。');return blindCountPage(data,context)}
  if(context.paperSessionId)return paperGuidePage(data,{context,workflowSettings});
  if(context.detailSessionId)return countDetailPage(data,{context,workflowSettings,role});
  if(access.kind==='manager')return managerCountPage(data,{workflowSettings,context});
  if(access.kind==='logistics')return logisticsCountPage(data);
  if(access.kind==='owner')return ownerCountPage(data,{workflowSettings});
  return staffCountPage(data,{workflowSettings});
}
