import{emptyState,escapeHtml}from'../components/layout.js';
export function receivingPage(rows){return `<h2 class="page-title">進貨</h2><section class="card"><h3>上傳真實收據</h3><p class="muted">原圖存入私有 Storage 後即可離開，OCR 在背景處理。</p><form id="receipt-upload"><input name="files" type="file" accept="image/*,application/pdf" multiple required><button class="primary" type="submit">上傳並排入 OCR</button><p class="error" data-error></p></form></section><h3>每日工作台</h3>${rows.length?`<div class="list">${rows.map(x=>`<article class="card"><strong>${escapeHtml(x.batch_number)}</strong><p class="muted">${escapeHtml(x.work_date||'')}｜${escapeHtml(x.status)}｜${x.receipt_documents?.length||0} 張</p></article>`).join('')}</div>`:emptyState('尚無正式收據','上傳後才會建立正式 batch；不會載入示範收據。')}`}
